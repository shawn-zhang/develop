#include <ctype.h>
#include <stdio.h>

int main(int argc, char *argv[])
{

    if (isalpha('(') == 0){
        printf("is not a alpha\n");
    
    }else
        printf("is a alpha\n");
    

    return 0;
}
